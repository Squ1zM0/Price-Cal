# Client-Only Gate Configuration
# This password is used for the initial device setup.
# WARNING: This value will be exposed in the client bundle - it's a convenience lock, not secure authentication.
NEXT_PUBLIC_GATE_PASSWORD=PASSWORD123$
