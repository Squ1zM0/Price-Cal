# Gate Password
# This is the password required to access the app
# Set this in your environment (Vercel, .env.local, etc.)
# Example for development: GATE_PASSWORD=Soccer123$
# IMPORTANT: Never commit the actual password to the repository
GATE_PASSWORD=

# WebAuthn/Face ID Configuration (Optional)
# These are only needed if you're deploying to a custom domain or non-Vercel environment
# If not set, the app will automatically use VERCEL_URL (on Vercel) or localhost (development)

# NEXT_PUBLIC_RP_ID: The Relying Party ID for WebAuthn (domain without protocol)
# Examples: "example.com" or "localhost"
# NEXT_PUBLIC_RP_ID=

# NEXT_PUBLIC_ORIGIN: The full origin URL for WebAuthn (with protocol)
# Examples: "https://example.com" or "http://localhost:3000"
# NEXT_PUBLIC_ORIGIN=

# NEXT_PUBLIC_RP_NAME: The human-readable name shown during Face ID prompts (Optional)
# Default: "Price Calculator"
# NEXT_PUBLIC_RP_NAME=

